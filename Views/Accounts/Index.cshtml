@model IEnumerable<NNTracker.Models.Account>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>

<form asp-controller="Account" asp-action="Index">
    <div class="input-group md-form form-sm form-2 pl-0">
        <input class="form-control my-0 py-1 red-border" name="searchText" type="text" placeholder="Search Account" />
        <div class=" input-group-append ">

            <span class=" input-group-text red lighten-3 " id="basic-text1">

                <button type="submit" class="btn btn-search" aria-label="Search">
                    <i class="bi bi-search"></i>
                </button>

            </span>

        </div>
    </div>
</form>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Account_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Account_Notes)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Initial_Budget)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Account_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Account_Notes)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Initial_Budget)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Account_Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Account_Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Account_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
<p class="col-0 text-end">
    <a asp-action="Index" class="btn btn-dark"><i class="bi bi-arrow-left-circle-fill"></i></a>
</p>
